[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python server.py"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "polyai-agent-studio-mcp"
